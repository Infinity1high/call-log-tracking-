{
  "name": "call-log-app",
  "version": "1.0.0",
  "description": "Application to monitor call stat",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Diana",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
